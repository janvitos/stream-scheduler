# Dependencies
node_modules/

# Claude Code project files (personal memory, local settings)
.claude/

# User data and config (contains credentials, personal schedules, M3U cache)
data/

# Bundled binary (users place this manually per bin/README.txt)
bin/ffmpeg.exe

# FFmpeg log files
logs/
